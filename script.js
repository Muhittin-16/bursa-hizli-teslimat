
/* ============================================================ BURSA HIZLI TESLİMAT Mobil teslimat sistemi + Supabase + WhatsApp + Otomatik Fiyat ============================================================ */ (() => { "use strict"; /* ============================================================ AYARLAR ============================================================ */ const SUPABASE_URL = "https://cwfkrwfvxurmtjkvllul.supabase.co"; const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_Kg3VNlkTYcjRDctC5RHPxA_eaX7YkCA"; const WHATSAPP_NUMBER = "905539497449"; const BUSINESS_PHONE = "05539497449"; let supabase = null; /* ============================================================ SABİT FİYAT TABLOSU ============================================================ NORMAL / ACİL Yıldırım : 500 / 750 TL Osmangazi : 750 / 1000 TL Gürsu : 1000 / 1250 TL Kestel : 1250 / 1500 TL Nilüfer : 1500 / 2000 TL ============================================================ */ const PRICE_TABLE = { yildirim: { name: "Yıldırım", normal: 500, acil: 750 }, osmangazi: { name: "Osmangazi", normal: 750, acil: 1000 }, gursu: { name: "Gürsu", normal: 1000, acil: 1250 }, kestel: { name: "Kestel", normal: 1250, acil: 1500 }, nilufer: { name: "Nilüfer", normal: 1500, acil: 2000 } }; /* ============================================================ SEÇİCİ ============================================================ */ const $ = (selector) => document.querySelector(selector); /* ============================================================ SUPABASE ============================================================ */ async function initSupabase() { if (supabase) { return supabase; } try { const module = await import( "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm" ); supabase = module.createClient( SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, { auth: { persistSession: false, autoRefreshToken: false } } ); return supabase; } catch (error) { console.error( "Supabase bağlantı hatası:", error ); throw error; } } /* ============================================================ TELEFON ============================================================ */ function cleanPhone(value) { return String(value || "").replace(/\D/g, ""); } function isValidTurkishPhone(value) { let digits = cleanPhone(value); if (digits.startsWith("90")) { digits = "0" + digits.substring(2); } if (!digits.startsWith("0")) { digits = "0" + digits; } return /^05\d{9}$/.test(digits); } function formatTurkishPhone(value) { let digits = cleanPhone(value); if ( digits.startsWith("90") && digits.length === 12 ) { digits = "0" + digits.substring(2); } if (digits.length > 11) { digits = digits.substring(0, 11); } if (!digits) { return ""; } let result = digits.substring(0, 4); if (digits.length > 4) { result += " " + digits.substring(4, 7); } if (digits.length > 7) { result += " " + digits.substring(7, 9); } if (digits.length > 9) { result += " " + digits.substring(9, 11); } return result; } /* ============================================================ SİPARİŞ NUMARASI ============================================================ */ function generateOrderNumber() { try { const array = new Uint8Array(4); crypto.getRandomValues(array); const randomPart = Array.from(array) .map((byte) => byte.toString(16).padStart(2, "0") ) .join("") .toUpperCase(); return "BHT-" + randomPart; } catch (error) { const randomPart = Math.random() .toString(16) .substring(2, 10) .toUpperCase(); return "BHT-" + randomPart; } } /* ============================================================ FİYAT FORMAT ============================================================ */ function formatPrice(value) { const number = Number(value); if ( !Number.isFinite(number) || number <= 0 ) { return ""; } return ( number.toLocaleString("tr-TR", { minimumFractionDigits: 0, maximumFractionDigits: 2 }) + " TL" ); } /* ============================================================ BÖLGE ADI ============================================================ */ function getDistrictName(districtKey) { const district = PRICE_TABLE[districtKey]; return district ? district.name : ""; } /* ============================================================ OTOMATİK FİYAT HESAPLAMA ============================================================ */ function calculateEstimatedPrice( district, urgency ) { const districtKey = String(district || "") .trim() .toLocaleLowerCase("tr-TR"); if (!districtKey) { return { price: null, district: null, districtName: "", urgency: null, reason: "Lütfen hizmet bölgesi seçiniz." }; } const districtData = PRICE_TABLE[districtKey]; if (!districtData) { return { price: null, district: null, districtName: "", urgency: null, reason: "Geçerli bir hizmet bölgesi seçiniz." }; } const urgencyKey = String(urgency || "Normal") .toLocaleLowerCase("tr-TR") === "acil" ? "acil" : "normal"; const price = Number(districtData[urgencyKey]) || 0; if (price <= 0) { return { price: null, district: districtKey, districtName: districtData.name, urgency: urgencyKey, reason: "Bu bölge için fiyat bulunamadı." }; } return { price, district: districtKey, districtName: districtData.name, urgency: urgencyKey }; } /* ============================================================ ESKİ SİSTEMLE UYUMLULUK ============================================================ */ function loadPricing() { pricingLoaded = true; updatePricePreview(); return Promise.resolve(true); } let pricingLoaded = true; /* ============================================================ FİYAT ÖNİZLEME ============================================================ */ function updatePricePreview() { const preview = $("#pricePreview"); const priceElement = $("#pricePreviewValue"); const districtField = $("#hizmetBolgesi"); const urgencyField = $("#urgency"); if ( !preview || !priceElement || !districtField || !urgencyField ) { return; } const district = districtField.value || ""; const urgency = urgencyField.value || "Normal"; if (!district) { priceElement.textContent = "Bölge seçiniz"; preview.classList.remove( "price-ready" ); return; } const result = calculateEstimatedPrice( district, urgency ); if (!result.price) { priceElement.textContent = "Fiyat bulunamadı"; preview.classList.remove( "price-ready" ); return; } priceElement.textContent = formatPrice(result.price); preview.classList.add( "price-ready" ); } /* ============================================================ WHATSAPP MESAJI ============================================================ */ function buildWhatsAppMessage(data) { const lines = [ "🚚 BURSA HIZLI TESLİMAT", "", "📦 YENİ TESLİMAT TALEBİ", "", `🔢 Sipariş No: ${ data.siparis_no || "-" }`, `👤 Ad Soyad: ${ data.ad_soyad || "-" }`, `📞 Telefon: ${ data.telefon || "-" }`, "", `🏍️ Hizmet Bölgesi: ${ data.hizmet_bolgesi_adi || getDistrictName( data.hizmet_bolgesi ) || "-" }`, `⚡ Aciliyet: ${ data.aciliyet === "acil" ? "Acil" : "Normal" }`, `💰 Teslimat Ücreti: ${ data.tahmini_fiyat ? formatPrice(data.tahmini_fiyat) : "-" }`, "", "📍 Alınacak Adres:", data.alis_adresi || "-", "", "🏁 Teslim Edilecek Adres:", data.teslimat_adresi || "-", "", `📦 Paket Türü: ${ data.paket_tipi || "Belirtilmedi" }`, "", `📝 Açıklama: ${ data.aciklama || "Yok" }`, `💬 Ek Not: ${ data.notlar || "Yok" }`, "", "Lütfen teslimat talebini kontrol ediniz." ]; return lines.join("\n"); } /* ============================================================ WHATSAPP AÇ ============================================================ */ function openWhatsApp(data) { const message = buildWhatsAppMessage(data); const url = `https://wa.me/${WHATSAPP_NUMBER}?text=` + encodeURIComponent(message); window.open( url, "_blank", "noopener,noreferrer" ); } /* ============================================================ FORM MESAJI ============================================================ */ function showFormMessage( message, type = "success" ) { const element = $("#formMessage"); if (!element) { return; } element.textContent = message; element.classList.add("show"); if (type === "error") { element.style.background = "rgba(255,98,98,.10)"; element.style.borderColor = "rgba(255,98,98,.30)"; element.style.color = "#ff9b9b"; } else { element.style.background = "rgba(25,195,125,.10)"; element.style.borderColor = "rgba(25,195,125,.30)"; element.style.color = "#83f0be"; } } /* ============================================================ TESLİMAT FORMU ============================================================ */ async function handleDeliverySubmit(event) { event.preventDefault(); const form = event.currentTarget; const name = $("#name")?.value.trim() || ""; const phoneRaw = $("#phone")?.value.trim() || ""; const pickup = $("#pickup")?.value.trim() || ""; const delivery = $("#delivery")?.value.trim() || ""; const district = $("#hizmetBolgesi")?.value || ""; const packageType = $("#packageType")?.value || ""; const urgency = $("#urgency")?.value || "Normal"; const description = $("#description")?.value.trim() || ""; const note = $("#note")?.value.trim() || ""; /* ========================================================== VALIDASYON ========================================================== */ if (name.length < 2) { showFormMessage( "Lütfen ad ve soyad bilgilerinizi girin.", "error" ); $("#name")?.focus(); return; } if (!isValidTurkishPhone(phoneRaw)) { showFormMessage( "Lütfen geçerli bir Türkiye cep telefonu numarası girin.", "error" ); $("#phone")?.focus(); return; } if (pickup.length < 5) { showFormMessage( "Lütfen alınacak adresi eksiksiz girin.", "error" ); $("#pickup")?.focus(); return; } if (delivery.length < 5) { showFormMessage( "Lütfen teslim edilecek adresi eksiksiz girin.", "error" ); $("#delivery")?.focus(); return; } if (!district) { showFormMessage( "Lütfen hizmet bölgesini seçin.", "error" ); $("#hizmetBolgesi")?.focus(); return; } /* ========================================================== TELEFON ========================================================== */ const phone = formatTurkishPhone(phoneRaw); /* ========================================================== FİYAT HESAPLA ========================================================== */ const priceResult = calculateEstimatedPrice( district, urgency ); const estimatedPrice = priceResult?.price || null; if (!estimatedPrice) { showFormMessage( "Seçtiğiniz bölge için fiyat hesaplanamadı.", "error" ); return; } /* ========================================================== BUTON ========================================================== */ const submitButton = form.querySelector( 'button[type="submit"]' ); const originalButtonText = submitButton ? submitButton.innerHTML : ""; if (submitButton) { submitButton.disabled = true; submitButton.innerHTML = "⏳ Talep Oluşturuluyor..."; submitButton.style.opacity = "0.7"; submitButton.style.pointerEvents = "none"; } try { showFormMessage( "Teslimat talebiniz kaydediliyor..." ); const client = await initSupabase(); /* ======================================================== SİPARİŞ NUMARASI ======================================================== */ const siparisNo = generateOrderNumber(); /* ======================================================== BÖLGE ADI ======================================================== */ const districtName = getDistrictName(district); /* ======================================================== PAYLOAD ======================================================== */ const payload = { siparis_no: siparisNo, ad_soyad: name, telefon: phone, alis_adresi: pickup, teslimat_adresi: delivery, hizmet_bolgesi: district, paket_tipi: packageType || null, aciliyet: String(urgency) .toLocaleLowerCase("tr-TR") === "acil" ? "acil" : "normal", aciklama: description || null, notlar: note || null, durum: "bekliyor", fiyat: estimatedPrice }; /* ======================================================== SUPABASE INSERT SELECT YOK ======================================================== */ const { error } = await client .from("teslimat_talepleri") .insert([payload]); if (error) { console.error( "Teslimat kayıt hatası:", error ); throw error; } /* ======================================================== BAŞARILI ======================================================== */ let successMessage = `✅ Talebiniz oluşturuldu. Sipariş Numaranız: ${siparisNo}`; successMessage += ` • ${districtName}`; successMessage += ` • ${formatPrice(estimatedPrice)}`; showFormMessage( successMessage ); /* ======================================================== WHATSAPP ======================================================== */ openWhatsApp({ ...payload, tahmini_fiyat: estimatedPrice, hizmet_bolgesi_adi: districtName }); /* ======================================================== FORM TEMİZLE ======================================================== */ form.reset(); updatePricePreview(); /* ======================================================== BUTON ======================================================== */ if (submitButton) { submitButton.innerHTML = "✅ Talep Oluşturuldu"; } setTimeout(() => { if (submitButton) { submitButton.innerHTML = originalButtonText; submitButton.disabled = false; submitButton.style.opacity = ""; submitButton.style.pointerEvents = ""; } }, 3500); } catch (error) { console.error( "Teslimat hatası:", error ); showFormMessage( "Teslimat talebi kaydedilemedi. Lütfen internet bağlantınızı kontrol edip tekrar deneyin.", "error" ); if (submitButton) { submitButton.innerHTML = originalButtonText; submitButton.disabled = false; submitButton.style.opacity = ""; submitButton.style.pointerEvents = ""; } } } /* ============================================================ TELEFON FORMAT ============================================================ */ function setupPhoneFormatting() { const phoneInput = $("#phone"); if (!phoneInput) { return; } phoneInput.addEventListener( "input", () => { phoneInput.value = formatTurkishPhone( phoneInput.value ); try { phoneInput.setSelectionRange( phoneInput.value.length, phoneInput.value.length ); } catch (_) {} } ); } /* ============================================================ FİYAT ALANLARINI DİNLE ============================================================ */ function setupPricingListeners() { const district = $("#hizmetBolgesi"); const urgency = $("#urgency"); if (district) { district.addEventListener( "change", updatePricePreview ); } if (urgency) { urgency.addEventListener( "change", updatePricePreview ); } } /* ============================================================ FORM ============================================================ */ function setupDeliveryForm() { const form = $("#deliveryForm"); if (!form) { return; } form.addEventListener( "submit", handleDeliverySubmit ); updatePricePreview(); } /* ============================================================ İLETİŞİM LİNKLERİ ============================================================ */ function setupContactLinks() { document .querySelectorAll( 'a[href^="tel:"]' ) .forEach((link) => { link.addEventListener( "click", () => { console.log( "Telefon aranıyor:", BUSINESS_PHONE ); } ); }); } /* ============================================================ SMOOTH SCROLL ============================================================ */ function setupSmoothScroll() { document .querySelectorAll( 'a[href^="#"]' ) .forEach((link) => { link.addEventListener( "click", (event) => { const targetId = link.getAttribute("href"); if ( !targetId || targetId === "#" ) { return; } const target = document.querySelector( targetId ); if (!target) { return; } event.preventDefault(); target.scrollIntoView({ behavior: "smooth", block: "start" }); } ); }); } /* ============================================================ HEADER ============================================================ */ function setupHeader() { const header = document.querySelector( ".site-header" ); if (!header) { return; } const updateHeader = () => { if (window.scrollY > 20) { header.style.boxShadow = "0 10px 35px rgba(0,0,0,.22)"; } else { header.style.boxShadow = ""; } }; window.addEventListener( "scroll", updateHeader, { passive: true } ); updateHeader(); } /* ============================================================ REVEAL ============================================================ */ function setupReveal() { const elements = document.querySelectorAll( ".step-card, .service-card, .contact-card, .quick-item, .hero-card" ); if (!elements.length) { return; } if ( !("IntersectionObserver" in window) ) { return; } const observer = new IntersectionObserver( (entries) => { entries.forEach( (entry) => { if ( !entry.isIntersecting ) { return; } entry.target.style.opacity = "1"; entry.target.style.transform = "translateY(0)"; observer.unobserve( entry.target ); } ); }, { threshold: 0.08 } ); elements.forEach( (element) => { element.style.opacity = "0"; element.style.transform = "translateY(16px)"; element.style.transition = "opacity .55s ease, transform .55s ease"; observer.observe( element ); } ); } /* ============================================================ BAŞLAT ============================================================ */ async function init() { setupDeliveryForm(); setupPhoneFormatting(); setupPricingListeners(); setupContactLinks(); setupSmoothScroll(); setupHeader(); setupReveal(); updatePricePreview(); console.log( "🚚 Bursa Hızlı Teslimat sistemi hazır." ); } /* ============================================================ GLOBAL API ============================================================ */ window.BursaHizliTeslimat = { buildWhatsAppMessage, isValidTurkishPhone, formatTurkishPhone, generateOrderNumber, calculateEstimatedPrice, loadPricing, openWhatsApp, getSupabase: () => supabase }; /* ============================================================ DOM READY ============================================================ */ if ( document.readyState === "loading" ) { document.addEventListener( "DOMContentLoaded", init ); } else { init(); } })();
